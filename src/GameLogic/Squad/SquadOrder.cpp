#include "GameLogic/Squad/SquadOrder.h"

bool Squads::SquadOrder::isPossible() {
    for(std::pair<Units::Unit*, int> order : orders) {
        //if(order.first -> )
    }
    return true;
}

void Squads::SquadOrder::enable() {}
void Squads::SquadOrder::disable() {}
