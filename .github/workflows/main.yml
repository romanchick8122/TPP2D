name: Doxygen Action
on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [master, dev]
jobs:
  generateDocs:
    runs-on: ubuntu-latest
    steps:
      - uses: mattnotmitt/doxygen-action@v1.2.3
